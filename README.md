# daos-userdata


### Version 0.01
* **Added version tracking:** Added a version number for human readability to easily compare changes between different versions (not used by the program itself).
* **Added configuration key:** Added the `dagatewayurl` configuration key to allow users to set the gateway URL.
